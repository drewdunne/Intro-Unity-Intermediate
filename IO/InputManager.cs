using System;
using System.Collections.Generic;
using System.Text;

namespace TicTacToeGame.IO
{
    class InputManager
    {
        public string PromptForInput(string prompt)
        {
            // Sends a prompt via OutputManager and returns an input from player.
            throw new NotImplementedException();
        }

        public string CheckForCommand()
        {
            throw new NotImplementedException();
        }
    }
}
